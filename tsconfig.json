{
  "compilerOptions": {
    "target": "es6",                          /* Especifica o padrão ECMAScript targetado. */
    "module": "commonjs",                     /* Especifica o sistema de módulos. */
    "rootDir": "./src",                       /* Raiz dos arquivos de entrada. */
    "outDir": "./dist",                       /* Diretório de saída para os arquivos compilados. */
    "strict": true,                           /* Ativa todas as verificações de tipo estritas. */
    "esModuleInterop": true,                  /* Habilita a interoperabilidade com módulos ES. */
    "skipLibCheck": true                      /* Pula a checagem de tipos dos arquivos de declaração. */
  },
  "include": ["src"],                         /* Inclui todos os arquivos TypeScript do diretório src. */
  "exclude": ["node_modules"]                 /* Exclui a pasta node_modules. */
}
